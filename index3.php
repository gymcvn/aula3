<!DOCTYPE html>
    <html>
       <body>

       <?php
       $a = 1; // A variavel $a é igual a 1
       $a += 2; // Somamos ao valor sw $a;
       echo $a;
       ?>

       <?php
       $a -= 2; //Subtraimos 2 ao valor de variavel $a;
       $a *= 2; // Multiplicamos o valor da variavel $a por 2;
       $a /= 2; // Dividimos o valor da variavel $a por 2.
       ?>

       <?php
       $a =1;
       echo ++$a; //Incrementamos 1 e retormamos valor;
       echo $a++; //Retormamos o valor e incrementamos 1
       echo --$a; // Decrementamos 1 e retormamos o valor
       echo $a; // Retormamos o valor e decrementamos 1
       ?>
       </body>
    </html>


